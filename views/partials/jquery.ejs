<!-- Bootstrap core JavaScript -->
<script src="vendor/jquery/jquery.min.js"></script>
<script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

<!-- Move geometry -->
<script>
  $(window).scroll(() => {
    var yoff = window.pageYOffset;
    // Added x-component and square translate
    $(".bg-circle").map((i, a) => {
      $(a).css('transform', 'translate(' + (i) / 10 * yoff + 'px,' + (i + 1) / 6 * yoff + 'px)');
    })

    $(".bg-square").map((i, a) => {
      $(a).css('transform', 'translate(' + (i - 2) / 12 * yoff + 'px,' + (2 * i - 1) / 6 * yoff + 'px)');
    })
  });
</script>

<!-- Dynamically resize footer elements -->
<!-- TODO: find better way to do this -->
<script>
  $(window).on('load resize', () => {
    // Width with whitespace
    var width = $('.partners').first().width() - 120;

    // Added x-component and square translate
    $(".logo").map((i, a) => {
      $(a).css('height', 'calc(' + width + 'px / 16)');
      $(a).css('margin', 'calc(' + width + 'px / 64)');
    })

    $(".pad-right-logo").map((i, a) => {
      $(a).css('padding-right', 'calc(' + width + 'px / 14)');
    })

    $(".spacer").map((i, a) => {
      $(a).css('font-size', 'calc(' + width + 'px * 5 / 64)');
      $(a).css('margin-bottom', 'calc(' + width + 'px / 64)');
    })
  });
</script>
