<!-- Bootstrap core JavaScript -->
<script src="vendor/jquery/jquery.min.js"></script>
<script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

<!-- Move geometry -->
<script>
  $(window).scroll(() => {
    var yoff = window.pageYOffset;
    // Added x-component and square translate
    $(".bg-circle").map((i, a) => {
      $(a).css('transform', 'translate(' + (i) / 10 * yoff + 'px,' + (i + 1) / 6 * yoff + 'px)');
    })

    $(".bg-square").map((i, a) => {
      $(a).css('transform', 'translate(' + (i - 2) / 12 * yoff + 'px,' + (2 * i - 1) / 6 * yoff + 'px)');
    })
  });
</script>

<!-- Dynamically resize footer elements -->
<!-- TODO: find better way to do this -->
<script>
  $(window).on('load resize', () => {
    var width = $('.partners').first().width();
    // Added x-component and square translate
    $(".logo").map((i, a) => {
      $(a).css('height', 'calc(' + width + 'px / 18)');
    })

    $(".pad-right-logo").map((i, a) => {
      $(a).css('padding-right', 'calc(' + width + 'px / 16)');
    })

    $(".spacer").map((i, a) => {
      $(a).css('font-size', 'calc(' + width + 'px * 5 / 72)');
      $(a).css('margin-bottom', 'calc(' + width + 'px / 72)');
    })
  });
</script>
